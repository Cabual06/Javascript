let x =3;
let y = 4;
let z;

z = Math.round(x);              // Round
z = Math.floor(x);              // Round down
z = Math.ceil(x);               // Round up
z = Math.trunc(x);              // Truncate, eliminate any decimal point
z = Math.pow(x);                // Raise to given power
z = Math.sqrt(x);               // Square root
z = Math.log(x);                // logarithm
z = Math.sin(x);                //  
z = Math.cos(x);                //
z = Math.tan(x);                //
z = Math.abs(x);                // Eliminate negative sign
let max = Math.max(x, y, z);    // Select the max 
let min = Math.min(x, y, z);    //  Select the min

console.log(z);